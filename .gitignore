node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files hold the database URL and JWT_SECRET. Only the annotated
# template is tracked; anything with real values stays local.
.env
.env.*
!.env.example

# Never commit local signing keys or machine-specific Android settings.
**/keystore.properties
**/local.properties
**/*.jks
**/*.keystore
**/*.pem
**/*.key

# Test and coverage output
coverage
*.log
